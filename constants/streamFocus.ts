/** Cross-fade when stream viewport highlight moves (ms); 0 when reduce-motion. */
export const STREAM_FOCUS_OPACITY_DURATION_MS = 280;

/** Stream row press shade fade (ms). */
export const STREAM_ROW_PRESS_IN_MS = 100;
export const STREAM_ROW_PRESS_OUT_MS = 180;
