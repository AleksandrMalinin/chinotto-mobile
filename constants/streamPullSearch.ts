/** Negative scroll Y (px) at top — pull past this to enter search recall on release. */
export const STREAM_PULL_SEARCH_COMMIT_PX = 52;

/** Drag must begin at or above this Y — ignores bounce when scrolling up from below. */
export const STREAM_PULL_SEARCH_TOP_EPSILON_PX = 12;

/** Pull distance before the hint begins to appear. */
export const STREAM_PULL_SEARCH_HINT_PX = 10;

/** Pull range mapped to hint progress 0 → 1. */
export const STREAM_PULL_SEARCH_PROGRESS_RANGE_PX = 56;
