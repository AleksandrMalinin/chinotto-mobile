/** Swipe-right reveal width for related-thought thread peel. */
export const STREAM_THREAD_PEEL_ACTION_WIDTH = 112;

/** Max neighbor rows shown in peel (not a feed). */
export const STREAM_THREAD_PEEL_MAX_NEIGHBORS = 2;
