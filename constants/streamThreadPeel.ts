/** Swipe-right reveal width for related-thought thread peel. */
export const STREAM_THREAD_PEEL_ACTION_WIDTH = 96;

/** One related row in peel — sheet rail holds the full thread. */
export const STREAM_THREAD_PEEL_MAX_NEIGHBORS = 1;
